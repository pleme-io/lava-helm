# lava-helm

Autogenerated typed `(deflava-resource …)` shapes for every helm
terraform-provider resource. Mirrors pangea-helm.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
